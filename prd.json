{
  "project": "Talon Rust Port",
  "branchName": "main",
  "description": "Complete the Rust port of Talon (Obsidian vault search engine) from where Phase 4 ended. Phases 0-4 (workspace, store, search algorithms, indexer/sync, embed pipeline, sqlite-vec) are landed with 206 passing tests. Remaining work: query handlers (search/read/related/meta/changes/lint/status), stateless MCP server, TS wrapper with per-platform binaries, parity tests, and final polish. Reference TS at /tmp/talon-scaffold-imports/ultra/edge/src/services/talon/. Architectural decision LOCKED per docs/superpowers/specs/2026-04-25-talon-extraction-design.md §2/§4: Talon is stateless and manual — NO file watcher, NO scheduler, NO clock-driven background work in Rust. The ultraclaw TS watcher remains the caller and spawns `talon sync`. `--mcp` is a stateless stdio request/response server, not a daemon. Spec also locks: §5 binary surface lists no `talon embed` command (embed runs inside `talon sync`, `--fast` skips it); Decision 8 locks the response envelope as `{action, version, ok, data, meta}` for success and `{action, version, ok: false, error}` for failures; Decision 12 puts the agent skill at `skill/SKILL.md` at repo root (not docs/); Decision 14 names the npm package `@seanmozeik/talon` with platform subpackages `@seanmozeik/talon-<platform>-<arch>`. Standing rules: never silence clippy lints with #[allow] (real fixes only); use wiremock for sidecar HTTP tests; real rusqlite Connection for DB tests (no mocks); pin deps to full semver; project conventions are eyre/color-eyre, time (not chrono), fs-err (not std::fs), serde_yaml_ng, blocking rusqlite + reqwest::blocking + spawn_blocking.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Add LLM expansion HTTP client",
      "description": "As Talon, I want a blocking OpenAI-compatible chat client so the hybrid search pipeline can request query expansion variants from the local LLM.",
      "acceptanceCriteria": [
        "New module talon-core/src/expansion/{mod,client,types,error}.rs",
        "Public surface: ExpansionClient::new(base_url, model) and expand(query: &str, n_variants: u8) -> Result<Vec<String>, ExpansionError>",
        "POSTs /chat/completions with system prompt ported from search/hybrid-expand.ts",
        "Returns empty Vec on malformed JSON (never panics); HTTP errors map to ExpansionError",
        "PII redaction reuses inference::redact",
        "Wiremock tests: happy path, malformed-JSON fallback, HTTP 5xx",
        "just verify passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Implemented talon-core/src/expansion/{mod,client,types,error}.rs. ExpansionClient::new(base_url, model) + expand(query, n_variants) -> Result<Vec<String>, ExpansionError>. Posts /chat/completions with system prompt from local-llm.ts. Malformed JSON returns Ok(vec![]). HTTP errors map to ExpansionError::Http. PII redaction reuses inference::redact. Wiremock tests cover happy path, malformed-JSON fallback, and HTTP 5xx. 212 tests pass."
    },
    {
      "id": "US-002",
      "title": "Add persistent llm_cache with TTL",
      "description": "As Talon, I want a SQL-backed cache for LLM expansion responses so repeated queries skip the LLM round trip across process restarts.",
      "acceptanceCriteria": [
        "New module talon-core/src/expansion/cache.rs",
        "Public surface: LlmCache::new(conn), get(key), put(key, value, ttl_ms), purge_expired()",
        "Uses existing llm_cache table from migrations (key, value, expires_at_ms columns)",
        "Tests: round-trip, expiry past TTL returns None, purge_expired removes stale rows",
        "just verify passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Implemented talon-core/src/expansion/cache.rs: LlmCache<'conn> with new(conn), get(key), put(key, value, ttl_ms), purge_expired(). Updated llm_cache migration to (key, value, expires_at_ms) schema. Exported from expansion::mod and lib.rs. 5 tests (round-trip, missing-key, expired-entry, overwrite, purge). 217 tests pass."
    },
    {
      "id": "US-003",
      "title": "Compose hybrid-single retrieval",
      "description": "As Talon, I want a single-query retrieval function that runs bm25 + fuzzy_title + vector in parallel and returns the three result lists for downstream RRF fusion.",
      "acceptanceCriteria": [
        "New module talon-core/src/search/hybrid_single.rs",
        "Public surface: run_hybrid_single(conn, query, embedding: Option<&[f32]>, limit) -> HybridSingleResult { bm25, fuzzy_title_parts: TitleSearchParts, vector }",
        "Composes existing search::bm25::search_bm25, search::fuzzy_title::search_title_parts, search::vector::search_vector — no new algorithm code",
        "Tests against seeded fixture: each retriever's contribution is isolated and complete",
        "just verify passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Implemented talon-core/src/search/hybrid_single.rs. HybridSingleResult { bm25, fuzzy_title_parts: TitleSearchParts, vector }. run_hybrid_single(conn, query, embedding: Option<&[f32]>, limit) composes search_bm25, search_title_parts, search_vector. embedding=None skips vector call and returns empty bucket. Re-exported HybridSingleResult and run_hybrid_single from search::mod. 5 tests: bm25/fuzzy-title/vector bucket isolation, None-embedding guard, disjoint-note independence. 222 tests pass."
    },
    {
      "id": "US-004",
      "title": "Build cross-encoder rerank pipeline",
      "description": "As Talon, I want a reranker that calls the sidecar /rerank endpoint and blends the cross-encoder scores into the hybrid scores using rank-tier weights.",
      "acceptanceCriteria": [
        "New module talon-core/src/search/rerank_pipeline.rs",
        "Public surface: rerank_candidates(inference, query, candidates: Vec<RawSearchResult>, top_k: u32) -> Vec<RawSearchResult>",
        "Calls inference.rerank(query, texts, return_text=false), then composes existing fuse::blend_rerank_candidates",
        "Honors RERANK_TOP_K from search/constants",
        "Wiremock test for /rerank; blend math matches TS expectations within 1e-4",
        "just verify passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Implemented talon-core/src/search/rerank_pipeline.rs. rerank_candidates(inference, query, candidates, top_k) calls inference.rerank with title+\"\\n\\n\"+snippet payload, composes fuse::blend_rerank_candidates. top_k caps candidates sent to reranker. Graceful degradation on HTTP error returns original candidates with hybrid scores unchanged. Added RERANK_TOP_K=40 to search/constants. 5 wiremock tests: happy path, blend math within 1e-4, HTTP 5xx fallback, empty-candidates early exit, top_k truncation. 227 tests pass."
    },
    {
      "id": "US-004b",
      "title": "Undo `talon embed` subcommand and fold the embed pass into `talon sync`",
      "description": "As a Talon operator, I want `talon sync` to embed pending chunks as part of the same call (per spec §5 line 188-204) so the binary surface matches the spec — `--fast` on sync skips the embed pass; there is no separate `talon embed` command. The previously-committed `talon embed` command violated the spec and must be removed.",
      "acceptanceCriteria": [
        "Delete the `\"embed\" => emit_embed(...)` arm from talon-cli/src/command.rs",
        "Delete emit_embed function entirely (do NOT leave it as a hidden alias)",
        "Remove EmbedInput / EmbedResponse from TalonInput / TalonResponse in talon-core/src/tool.rs (the embed action is not in the spec's MCP tool surface — Decision 10)",
        "Extend run_sync(conn, vault_root, lock_path, indexer_config, embed_config: Option<EmbedPassOptions>, inference: Option<&InferenceClient>) — when embed_config is Some AND inference is Some AND not --fast, run the embed pass after reconcile_deletions",
        "Extend SyncResponse with embed-pass fields: { embedded: u32, embed_failed: u32, dimension_mismatch: bool, embed_remediation: Option<String>, embed_diagnostics: Vec<String> } — fold the existing remediation hint here",
        "Update emit_sync in command.rs: when --fast is absent, build InferenceClient + EmbedPassOptions from config and pass through; --fast skips inference build entirely (no useless network setup)",
        "Update output.rs::emit_sync_human to print the embed sub-line (e.g. `Embedded N notes (M failed)` and the remediation hint when set)",
        "Existing wiremock embed tests in talon-core/src/embed/runner.rs continue to pass — they test run_embed_pass directly and don't depend on the CLI command",
        "Add an integration test crates/talon-core/tests/sync_embed.rs: temp vault → talon sync (with mocked sidecar) → vec_chunks populated; talon sync --fast → vec_chunks empty",
        "just verify passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Implemented: removed talon embed CLI subcommand and emit_embed handler. Removed EmbedInput/EmbedResponse from TalonInput/TalonResponse. Extended run_sync() to accept Option<EmbedPassOptions> + Option<&InferenceClient> — runs embed pass after reconciliation when both are Some. Extended SyncResponse with embedded/embed_failed/dimension_mismatch/embed_remediation/embed_diagnostics fields. Updated emit_sync to build InferenceClient from config when --fast absent. Updated emit_sync_human to print embed sub-line. Fixed vec_chunks INSERT in persist.rs (chunk_id not rowid for sqlite-vec virtual tables). Added 3 integration tests in crates/talon-core/tests/sync_embed.rs. 232 tests pass."
    },
    {
      "id": "US-005",
      "title": "Build hybrid-pipeline orchestrator",
      "description": "As Talon, I want the full hybrid search pipeline (probe, optional expansion, multi-variant retrieval, fuse, rerank, filter) wired together so the search command can call one function.",
      "acceptanceCriteria": [
        "New module talon-core/src/search/hybrid_pipeline.rs",
        "Public surface: run_hybrid_pipeline(conn, inference, expansion, query, options) -> Vec<RawSearchResult>",
        "Strong-signal short-circuit via fuse::estimate_strong_signal — skips expansion+rerank when probe is decisive",
        "Calls US-001 expansion + US-003 hybrid-single per variant + US-004 rerank",
        "Wiremock end-to-end test: full pipeline against seeded fixture vault with mocked sidecar (chat + embed + rerank)",
        "Strong-signal path verified: high-confidence query skips expansion call",
        "just verify passes"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Module talon-core/src/search/hybrid_pipeline.rs already implemented with 5 wiremock tests. Fixed clippy violations blocking verification: redundant clones in variant query assembly, uninlined format args in sync_embed.rs tests, #[ignore] without reason in bm25 diagnostic test. 232 tests pass."
    },
    {
      "id": "US-005b",
      "title": "Wrap TalonResponse in the spec-locked {action, version, ok, data, meta} envelope",
      "description": "As an MCP / agent client, I want every Talon response to use the envelope locked by Decision 8 — `{action, version, ok: true, data, meta}` for success, `{action, version, ok: false, error}` for failure — so downstream contracts are stable. The current `#[serde(tag = \"action\")]` flattens action-specific fields onto the top level with no `ok`, no `version`, no `meta`. The doc comment in tool.rs already declares this contract; the implementation must be brought in line.",
      "acceptanceCriteria": [
        "Replace TalonResponse enum with a TalonEnvelope struct: { action: String (kebab-case), version: String (env!(\"CARGO_PKG_VERSION\")), ok: bool, data: Option<TalonResponseData>, meta: Option<ResponseMeta>, error: Option<ErrorEnvelope> }",
        "TalonResponseData becomes the action-discriminated payload (the existing variants: Search, Read, Sync, Status, Related, Meta, Changes, Lint), serialized via #[serde(untagged)] OR a sibling tag — whichever yields the spec's nested `data: { ... }` shape",
        "Helper constructors: TalonEnvelope::ok(action, data, meta) and TalonEnvelope::err(action, error)",
        "All CLI command handlers in talon-cli/src/command.rs construct envelopes via the helpers — no raw struct literals",
        "All emit_*_human formatters in talon-cli/src/output.rs unwrap the envelope's data field before rendering",
        "JSON output (--json / --agent) emits the full envelope per spec",
        "Tests: serde round-trip for one variant of each shape; assert top-level keys are exactly action/version/ok/data/meta on success and action/version/ok/error on failure; existing CLI integration tests updated to read through envelope",
        "just verify passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Implemented TalonEnvelope struct replacing TalonResponse enum. Success shape: {action, version, ok: true, data, meta}. Error shape: {action, version, ok: false, error}. TalonResponseData tagged enum wraps all 8 response variants. Helper constructors TalonEnvelope::ok() and TalonEnvelope::err(). CLI command.rs uses envelope helpers everywhere. output.rs formatters unwrap data field. camelCase on ResponseMeta + ErrorEnvelope. ErrorCode added Display. 15 round-trip serde tests. 250 tests pass."
    },
    {
      "id": "US-006",
      "title": "Wire real search command",
      "description": "As a Talon CLI user, I want `talon search` to return real ranked results across hybrid/semantic/fulltext/title modes with scope-priority post-multiplication, plus --where and --since post-filtering.",
      "acceptanceCriteria": [
        "Replace talon-cli/src/command.rs::emit_search_stub with real handler calling US-005",
        "Modes: hybrid (default), semantic (vector only), fulltext (bm25 only), title (fuzzy_title only)",
        "Query embedding via blocking inference.embed for semantic/hybrid modes",
        "--where <KEY OP VALUE> (repeatable) post-filters results by frontmatter (reuses WhereClause/WhereOperator from tool.rs and the meta handler's filter primitive — coordinate with US-009)",
        "--since <TS> post-filters results to notes whose mtime >= parsed timestamp (reuses change_tracking::parse_since)",
        "Post-rerank: multiply each result.score by ScopePriority::multiplier(), then re-sort",
        "--fast flag skips expansion + skips rerank (per spec §5 line 225)",
        "SearchResponse::empty_scaffold call sites removed",
        "Integration test: end-to-end on temp vault (run_sync + search) with wiremock sidecar, including --where and --since cases",
        "Wraps result in the US-005b envelope",
        "just verify passes"
      ],
      "priority": 8,
      "passes": true,
      "notes": "Wired real search handler calling run_search() in talon-core/src/query/search.rs. Supports hybrid/semantic/fulltext/title modes. --where frontmatter filters and --since timestamp filters applied post-retrieval. ScopePriority::multiplier() multiplied post-rerank and results re-sorted. --fast skips expansion and rerank. Removed SearchResponse::empty_scaffold (no call sites remain). Changed SearchResult.score/raw_score from f32 to f64 to fix cast lints. Fixed 22 clippy violations. 6-test integration suite in tests/search_integration.rs. 256 tests pass."
    },
    {
      "id": "US-007",
      "title": "Build read handler",
      "description": "As an agent, I want `talon read <path>` to return the note body with frontmatter stripped, optional line range, and a heading tree.",
      "acceptanceCriteria": [
        "New module talon-core/src/query/read.rs with read_note(conn, vault_root, input: &ReadInput) -> ReadResponse",
        "Supports --from-line / --max-lines slicing",
        "Supports --raw mode (no frontmatter strip)",
        "Returns headings tree extracted from body",
        "Replaces talon-cli/src/command.rs::emit_read_stub",
        "Tests: read fixture note with frontmatter, line range, raw mode, missing file → InvalidInput",
        "just verify passes"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Implemented talon-core/src/query/read.rs: run_read(conn, vault_root, &ReadInput) -> ReadResponse. Queries notes table for vault_path + active=1. Strips frontmatter via parse_frontmatter unless raw=true. Applies 1-based from_line/max_lines slicing. Hydrates links (outgoing), backlinks, tags, aliases from relational tables. Missing notes return found: false; empty path returns empty results list. Replaced emit_read_stub in talon-cli/command.rs. Updated emit_read_human to render title/path/tags/links/body. Exported run_read from talon-core lib.rs. 5 tests: frontmatter strip, raw mode, line range, missing note, links+backlinks. 261 tests pass."
    },
    {
      "id": "US-008",
      "title": "Build related-notes handler",
      "description": "As an agent, I want `talon related <path>` to traverse the link graph in outgoing/backlinks/both directions up to a configured depth.",
      "acceptanceCriteria": [
        "New module talon-core/src/query/related.rs with find_related(conn, input: &RelatedInput) -> RelatedResponse",
        "Direction: Outgoing | Backlinks | Both; depth 1-3 (RELATED_DEFAULT_DEPTH=1)",
        "Reuses existing links::compute_backlinks and resolve_wiki_links",
        "Scope filter applied during traversal",
        "Replaces talon-cli/src/command.rs::emit_related_stub",
        "Tests: traversal on fixture graph (Graph/Parent → Graph/Child → Graph/Grandchild) with depth limits and direction variants",
        "just verify passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": "Implemented talon-core/src/query/related.rs: find_related(conn, &RelatedInput) -> RelatedResponse. BFS traversal with cycle detection (visited HashSet), supports Outgoing/Backlinks/Both direction, depth clamped to RELATED_MAX_DEPTH=3. Scope filter via passes_scope_filter applied per neighbor. Title hydrated from notes table. Fixed clippy manual_let_else lint. Replaced emit_related_stub in talon-cli/src/command.rs with real emit_related handler opening the index DB. Exported from query::mod and lib.rs. 5 tests: outgoing depth-1, outgoing depth-2 transitive, backlinks, both directions, orphan. 266 tests pass."
    },
    {
      "id": "US-009",
      "title": "Build meta (frontmatter query) handler",
      "description": "As an agent, I want `talon meta` to filter notes by frontmatter --where clauses, project --select fields, aggregate --tag-counts, and resolve --sources reverse references.",
      "acceptanceCriteria": [
        "New module talon-core/src/query/meta.rs with query_meta(conn, input: &MetaInput) -> MetaResponse",
        "--where operators: =, !=, <, <=, >, >=, contains, exists (existing WhereClause/WhereOperator types)",
        "--select projects requested frontmatter fields onto each entry",
        "--tag-counts emits SELECT tag, COUNT(*) FROM note_tags GROUP BY tag",
        "--sources resolves via ReverseSourceIndex",
        "--since applies time-window filter",
        "Replaces talon-cli/src/command.rs::emit_meta_stub",
        "Expose the WhereClause matcher as a reusable primitive (e.g. apply_where_clauses(rows, clauses) -> Vec<row>) so US-006 search --where can share the implementation rather than reimplement it",
        "Tests: each operator on fixture vault, tag aggregation, source lookup",
        "just verify passes"
      ],
      "priority": 11,
      "passes": true,
      "notes": "Implemented talon-core/src/query/meta.rs: query_meta(conn, &MetaInput) -> MetaResponse. Supports --where (8 operators via shared query/where_filter.rs primitives), --since timestamp filter, --select field projection from notes.frontmatter JSON, --tag-counts aggregation via note_tags, and --sources reverse-reference lookup via note_frontmatter_fields. CLI gains --select/--tag-counts/--sources flags via MetaArgs sub-struct. emit_meta replaces emit_meta_stub in command.rs. 8 unit tests. 274 tests pass."
    },
    {
      "id": "US-010",
      "title": "Build changes handler",
      "description": "As an agent, I want `talon changes --since <timestamp>` to return the added/modified/deleted notes from the event_log.",
      "acceptanceCriteria": [
        "New module talon-core/src/query/changes.rs with get_changes(conn, since: Option<i64>) -> ChangesResponse",
        "Returns { added, modified, deleted } populated from event_log table",
        "--since parsing reuses change_tracking::parse_since",
        "Replaces the bail!(\"changes is scaffolded but not implemented yet\") in command.rs",
        "Tests: simulate add → modify → delete sequence via direct DB writes; verify classification",
        "just verify passes"
      ],
      "priority": 12,
      "passes": true,
      "notes": "Implemented talon-core/src/query/changes.rs: query_changes(conn, &ChangesInput) -> ChangesResponse. Reads all event_log rows, parses RFC3339 timestamps to ms via rfc3339_to_ms helper, and classifies: added (first 'index' event in window), modified ('index' in window with prior 'index' before window), deleted ('delete' event in window). Reuses change_tracking::parse_since. Scope filter applied per-entry; results sorted by timestamp; limit respected. Replaced bail! stub in command.rs with emit_changes handler. Exported query_changes from query::mod and lib.rs; added ChangesInput to lib.rs pub use. 5 unit tests. 279 tests pass."
    },
    {
      "id": "US-011",
      "title": "Build lint handler",
      "description": "As an agent, I want `talon lint <check>` to surface graph health issues: orphans, broken-links, dangling-refs, unreferenced.",
      "acceptanceCriteria": [
        "New module talon-core/src/query/lint.rs with run_lint(conn, check: LintCheck) -> LintResponse",
        "Orphans: notes with zero incoming links",
        "BrokenLinks: link target file does not exist",
        "DanglingRefs: frontmatter reference to nonexistent path",
        "Unreferenced: zero incoming AND zero outgoing",
        "Replaces talon-cli/src/command.rs::emit_lint_stub",
        "Tests: each check on fixture vault (Graph/Grandchild=orphan, Lifecycle/Doomed→nonexistent=broken)",
        "just verify passes"
      ],
      "priority": 13,
      "passes": true,
      "notes": "Implemented talon-core/src/query/lint.rs: query_lint(conn, &LintInput) -> LintResponse. Four SQL-backed checks: orphans (no incoming links via NOT IN subquery on links.to_path), broken-links (links.to_path not in active notes), dangling-refs (note_frontmatter_fields values ending in .md not in active notes), unreferenced (neither incoming nor outgoing links). Scope filter applied to all checks. Replaced emit_lint_stub in talon-cli/src/command.rs with real emit_lint handler opening the index DB. Exported query_lint from query/mod.rs and lib.rs. 5 unit tests. 284 tests pass."
    },
    {
      "id": "US-012",
      "title": "Replace status stub with real index counts",
      "description": "As an operator, I want `talon status` to report real index health: active note count, chunk count, failed-embedding count, vector dimensions, scope report.",
      "acceptanceCriteria": [
        "Replaces StatusResponse::scaffold and command.rs::emit_status_stub",
        "Real counts via SQL: COUNT(*) FROM notes WHERE active=1, COUNT(*) FROM chunks, COUNT(*) FROM chunks WHERE embedding_status='failed'",
        "Vector dimensions via vec_ext::get_vec_chunks_dimensions",
        "Scope report: configured scopes, default set, unscoped count",
        "State: Ready when vault reachable + index exists; ConfigError when talon init not run",
        "Tests: status against seeded vault, status with no DB returns ConfigError",
        "just verify passes"
      ],
      "priority": 14,
      "passes": true,
      "notes": "Implemented talon-core/src/query/status.rs: query_status(conn, config) -> StatusResponse. SQL counts: active notes (WHERE active=1), total chunks, failed chunks (embedding_status='failed'). Vector dimensions via vec_ext::get_vec_chunks_dimensions. Scope report: total_scopes from config.scopes.len(), default_scopes from config.default_scope_names(), unscoped_count via glob matching against active note paths. emit_status in talon-cli/src/command.rs handles config/DB load failures gracefully (returns StatusState::ConfigError instead of propagating errors). Removed StatusResponse::scaffold(); added ContainerPath::root() infallible constructor; exported ScopeReport and query_status from lib.rs. 5 unit tests. 289 tests pass."
    },
    {
      "id": "US-013",
      "title": "Port fixture vault and add integration test suite",
      "description": "As a maintainer, I want the 21-note fixture vault from the TS reference ported into a real on-disk fixture so integration tests exercise the full query layer end-to-end.",
      "acceptanceCriteria": [
        "Port 21 notes from /tmp/talon-scaffold-imports/.../tests/fixtures/talon/fixture-vault.ts to crates/talon-core/tests/fixtures/vault/ (Atlas/*, Graph/*, Search/*, Filters/*, Lifecycle/*)",
        "New integration test crates/talon-core/tests/fixture_vault.rs",
        "Runs run_sync (which now also embeds per US-004b) with mocked sidecar, then exercises: search hybrid, search fulltext, related, meta-where, lint orphans, status",
        "Asserts paths exact-match TS expectations from tests/services/talon/fixture-vault.integration.test.ts; scores within 1e-3",
        "Suite green across 5 consecutive runs with no flake",
        "just verify passes"
      ],
      "priority": 15,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-014",
      "title": "Add MCP JSON-RPC stdio transport",
      "description": "As an MCP client, I want `talon --mcp` to speak JSON-RPC 2.0 over stdio so it can be embedded in MCP-aware agents.",
      "acceptanceCriteria": [
        "New module talon-cli/src/mcp/{mod,transport,protocol}.rs",
        "Choose between rmcp crate and hand-rolled JSON-RPC; document choice with reason in module docstring",
        "Frame format: line-delimited JSON-RPC 2.0 on stdin/stdout (no Content-Length headers)",
        "Methods supported: initialize, initialized notification, tools/list, tools/call, shutdown",
        "Tests: handshake echo round-trips; malformed frame returns proper JSON-RPC error envelope",
        "just verify passes"
      ],
      "priority": 16,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-015",
      "title": "Add MCP tool schema and handler dispatch",
      "description": "As an MCP client, I want a single `talon` tool whose action-discriminated input dispatches to the same query handlers the CLI uses.",
      "acceptanceCriteria": [
        "New module talon-cli/src/mcp/tool.rs",
        "Tool name: talon (per design spec §11)",
        "Action union per spec §11 / Decision 10: search, read, sync, related, status, meta, changes, lint — and ONLY these (no embed action)",
        "Input schema generated for each TalonInput variant (schemars crate or hand-written constants in tool.rs)",
        "tools/call dispatches through the same handlers as US-006 through US-012; wraps responses in the US-005b envelope as MCP CallToolResult",
        "TalonError mapped to MCP error envelope with code + message",
        "Tests: tools/list returns valid schema; each action's tools/call round-trips against the integration vault",
        "just verify passes"
      ],
      "priority": 17,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-016",
      "title": "Wire --mcp flag to stateless stdio request/response loop",
      "description": "As an MCP client, I want `talon --mcp` to read JSON-RPC frames from stdin and write responses to stdout until EOF or shutdown — stateless per call (open DB, answer, return), no clock-driven work, per spec §4.",
      "acceptanceCriteria": [
        "Replaces bail!(\"mcp mode is scaffolded but not implemented yet\") in command.rs",
        "Frame loop reads stdin until EOF or JSON-RPC shutdown method; SIGINT triggers clean exit",
        "Each tools/call dispatches to the same handler the CLI uses, opens DB if needed, returns response — no in-memory caching beyond the existing SearchCache LRU",
        "No background tasks, no timers, no fs watching inside the loop (spec §2 Decision 1)",
        "Integration test: spawn `talon --mcp` subprocess, send initialize + tools/list + tools/call + shutdown, verify responses and clean exit",
        "Manual smoke: `talon --mcp <<< '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"initialize\",\"params\":{}}'` returns valid response",
        "just verify passes"
      ],
      "priority": 18,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-017",
      "title": "Scaffold ts/ npm package @seanmozeik/talon with binary resolver",
      "description": "As a Node consumer, I want a `@seanmozeik/talon` npm package (per Decision 14) that resolves the right prebuilt binary at runtime via optionalDependencies, matching the esbuild/biome/oxc/ddg pattern.",
      "acceptanceCriteria": [
        "New ts/ workspace with package.json name `@seanmozeik/talon` declaring optionalDependencies: @seanmozeik/talon-darwin-arm64, @seanmozeik/talon-darwin-x64, @seanmozeik/talon-linux-x64, @seanmozeik/talon-linux-arm64, @seanmozeik/talon-win32-x64",
        "ts/src/binary.ts: resolveBinary() walks optionalDependencies matching process.platform + process.arch, returns the binary path via require.resolve('@seanmozeik/talon-<plat>-<arch>/bin/talon')",
        "ts/src/child.ts: mcpChildSpec(): { command, args: ['--mcp'], env: NodeJS.ProcessEnv } per design spec §9",
        "ts/src/index.ts barrel exports mcpChildSpec and resolveBinary (the only public API per spec §9 — no Talon class, no .search()/.sync() methods)",
        "Vitest tests for resolution logic with mocked platform/arch",
        "npm pack produces a valid tarball; npm test clean"
      ],
      "priority": 19,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-018",
      "title": "Cross-build per-platform binaries with cargo zigbuild",
      "description": "As a release engineer, I want `cargo zigbuild` to produce stripped release binaries for darwin-arm64, darwin-x64, linux-x64, linux-aarch64, win32-x64 so each @seanmozeik/talon-<platform>-<arch> npm subpackage ships a working binary.",
      "acceptanceCriteria": [
        "Document zig + cargo-zigbuild install in DEVELOPMENT.md (or equivalent)",
        "Build matrix script (justfile or shell) runs: cargo zigbuild --release --target aarch64-apple-darwin / x86_64-apple-darwin / x86_64-unknown-linux-gnu / aarch64-unknown-linux-gnu / x86_64-pc-windows-gnu",
        "Each target output is stripped and < 30 MB",
        "Each binary runs --version on its target platform (CI matrix or qemu/wine smoke)",
        "Per-platform npm subpackage layout: package.json (name `@seanmozeik/talon-<platform>-<arch>`) + the prebuilt binary at bin/talon, no JS",
        "just verify passes"
      ],
      "priority": 20,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-019",
      "title": "Add release workflow and version tagging",
      "description": "As a release engineer, I want a tag push to trigger the cross-build matrix and publish all platform packages plus the @seanmozeik/talon meta package to npm in one shot.",
      "acceptanceCriteria": [
        "New .github/workflows/release.yml triggered by v* tag push",
        "Matrix invokes US-018 build script, packages each platform binary, npm publish each @seanmozeik/talon-<platform>-<arch> subpackage and the @seanmozeik/talon meta package",
        "Version is read from Cargo.toml; ts/package.json version stays in lockstep",
        "Dry-run mode (workflow_dispatch with input) goes through every step except publish",
        "Dry-run succeeds end-to-end on a clean checkout"
      ],
      "priority": 21,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-020",
      "title": "Embed skill/SKILL.md and wire --skill flag",
      "description": "As an MCP client, I want `talon --skill` to print an embedded SKILL.md (located at repo root `skill/SKILL.md` per spec §3 + Decision 12) so agents can self-load Talon usage instructions, matching the ddg pattern.",
      "acceptanceCriteria": [
        "Author skill/SKILL.md at repo root (port the agent prompt from the TS reference, then extend to document the new meta/changes/lint actions per spec §12)",
        "include_str! embeds skill/SKILL.md into the talon-cli binary (path: include_str!(\"../../../skill/SKILL.md\") from talon-cli/src/main.rs)",
        "--skill prints the contents and exits 0",
        "Test: assert_cmd verifies non-empty output + exit 0",
        "just verify passes"
      ],
      "priority": 22,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-021",
      "title": "Polish human CLI output",
      "description": "As a CLI user, I want terminal-width-aware wrapping, colored headings, compact result cards, and a spinner during search so the human output is pleasant.",
      "acceptanceCriteria": [
        "Add terminal_size dep (full semver) if not present",
        "Search results render as compact cards: path, score, snippet — wrapped to terminal width",
        "Headings use existing anstyle/anstream styling",
        "Spinner from existing spinner module wired for search command",
        "Snapshot tests via insta (one snapshot per command's human output)",
        "just verify passes"
      ],
      "priority": 23,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-022",
      "title": "Add ranking regression test suite (parity bar §19 Phase 1)",
      "description": "As a maintainer, I want golden expected ranks for canonical queries so silent ranking drift is caught in CI — meeting the spec §19 Phase 1 acceptance bar (path equality, score within tolerance, stable subset of meta fields).",
      "acceptanceCriteria": [
        "Port tests/services/talon/ranking-regression.test.ts to crates/talon-core/tests/ranking_regression.rs",
        "Golden expected top-K paths for ~10 fixture queries — assert exact equality of data.results[*].path against the TS reference output",
        "Score drift on data.results[*].score tolerated within 1e-3; rank order changes fail the test",
        "Stable subset of envelope `meta` fields asserted (e.g. result_count, scope set, fast flag) — not the full meta blob",
        "Any TS-vs-Rust intentional divergence documented in a comment with reason",
        "Suite green; just verify passes"
      ],
      "priority": 24,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-023",
      "title": "Add CLI contract test suite",
      "description": "As a maintainer, I want assert_cmd-driven contract tests so the CLI's exit codes, help text, and JSON envelope shape are pinned and cannot regress silently.",
      "acceptanceCriteria": [
        "New crates/talon-cli/tests/cli.rs using assert_cmd + predicates",
        "Covers: help text shape, exit code per error class",
        "JSON envelope shape per Decision 8: top-level keys are exactly {action, version, ok, data, meta} on success and {action, version, ok, error} on failure — verified for every command",
        "Ports relevant assertions from tests/services/talon/cli.test.ts and contract.test.ts",
        "Suite green; just verify passes"
      ],
      "priority": 25,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-024",
      "title": "Final verification: release build, parity, README, manual smokes",
      "description": "As a release engineer, I want every shippable surface verified: release build, parity against TS for canonical queries, README explains the inference endpoint, manual smoke of every command — before tagging v0.1.0.",
      "acceptanceCriteria": [
        "just verify green",
        "cargo build --release succeeds; stripped binary < 30 MB",
        "talon --version, talon --help, talon --skill all work",
        "talon init creates ~/.config/talon/config.toml from template",
        "talon sync indexes a real vault end-to-end AND embeds pending chunks against a live TEI sidecar in the same call (per US-004b spec-faithful sync; the caller — ultraclaw's chokidar watcher or a launchd plist — is responsible for invoking sync; Rust does not watch)",
        "talon sync --fast completes without contacting the inference sidecar (lexical pass only)",
        "talon search '<query>' returns real ranked results with --where and --since filtering working (manual smoke)",
        "talon related <path>, talon meta --tag-counts, talon lint orphans all work on real data",
        "talon --mcp survives initialize + tools/list + tools/call + shutdown round-trip from an MCP inspector or hand-crafted JSON-RPC; tools/list does NOT advertise an embed action (Decision 10)",
        "README explains inference.base_url points at a TEI-compatible endpoint (text-embeddings-inference, Infinity, or ultraclaw's sidecar) per Decision 13",
        "JSON output for every command matches the {action, version, ok, data, meta} envelope shape (Decision 8) verified by US-023 contract tests",
        "Parity bar §19 Phase 1: 5+ canonical search queries return matching data.results[*].path against TS reference (top-K order must match; data.results[*].score drift within 1e-3 allowed; stable subset of meta fields equal)",
        "v0.1.0 tag pushed; US-019 release workflow publishes successfully (dry-run first if uncertain)"
      ],
      "priority": 26,
      "passes": false,
      "notes": ""
    }
  ]
}
